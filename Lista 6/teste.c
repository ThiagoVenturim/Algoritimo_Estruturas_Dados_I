#include <stdio.h>
#include<stdbool.h>

bool primoEntreSi(int num1, int num2);
bool sãoPrimos= true;
int maior = num2;
if (num1>num2) maior1=  num2;
for (int i =2; 1 < num2; i++){
if((num1 % i==0) && (num2%2==0)){
}
return primoEntreSi;
}
int main (){
int num1, num 2;
printf("Digite dois numeros: ");
scanf("Digite dois ");
primoEntreSi(num1, num2);
return 0;

}